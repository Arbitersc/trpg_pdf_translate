# ================================================
# 测试环境配置文件（示例）
# ================================================
# 用于 pytest 测试运行
# 此文件不包含真实的 API 密钥，测试使用 mock 模拟请求
# 复制此文件为 .env.test 并根据需要修改
# ================================================

# 环境标识
ENVIRONMENT=test

# LLM 提供商配置
# 测试时使用 openai 配置，但实际 API 调用会被 mock
LLM_PROVIDER=openai

# OpenAI 测试配置
# 使用虚假的 API 密钥，测试会 mock 实际请求
OPENAI_API_KEY=test-api-key-for-mocking
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Ollama 测试配置
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# Flask 测试配置
FLASK_DEBUG=false
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# PDF 测试配置
PDF_DIRECTORY=test_pdfs
